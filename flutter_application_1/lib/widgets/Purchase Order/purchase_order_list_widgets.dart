import 'package:flutter/material.dart';

class MobilePurchaseOrderListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Purchase Orders'),
      ),
      body: Center(
        child: Text('This is the mobile purchase order list screen.'),
      ),
    );
  }
  }